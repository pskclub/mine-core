# LF everywhere. gofmt, the postmangen golden fixtures and CI all compare bytes,
# and a CRLF checkout on Windows makes every one of them disagree with what the
# repository holds. Every text file is already committed with LF, so this rule
# changes no content; it only stops a Windows checkout from introducing CRLF.
* text=auto eol=lf

*.png binary
*.jpg binary
